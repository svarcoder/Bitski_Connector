import { AccessTokenProvider, BitskiEngine, Network } from 'bitski-provider';
import { ProviderOptions } from '../sdk';
import { BitskiTransactionSigner } from '../signing/transaction-signer';
export declare class BitskiBrowserEngine extends BitskiEngine {
    protected network: Network;
    protected webBaseUrl: string;
    protected apiBaseUrl: string;
    protected tokenProvider: AccessTokenProvider;
    protected clientId: string;
    protected sdkVersion: string;
    protected headers: Record<string, unknown>;
    protected rpcHeaders: Record<string, unknown>;
    protected signer: BitskiTransactionSigner;
    constructor(clientId: string, tokenProvider: AccessTokenProvider, sdkVersion: string, network: Network, options?: ProviderOptions, TransactionSignerClass?: typeof BitskiTransactionSigner);
    protected addSubproviders(): void;
}
