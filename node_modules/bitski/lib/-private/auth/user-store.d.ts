import { Store } from '../utils/store';
import { User } from './user';
export declare class UserStore {
    getCurrentUser(): Promise<User | undefined>;
    protected get storageKey(): string;
    protected store: Store;
    protected clientId: string;
    protected user: Promise<User | undefined>;
    constructor(clientId: string, store?: Store);
    set(user: User | undefined): Promise<void>;
    clear(): Promise<void>;
    loadUserFromCache(): void;
    protected fetchUser(): Promise<User | undefined>;
    protected cacheUser(user: User | undefined): Promise<void>;
}
